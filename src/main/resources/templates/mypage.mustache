{{>layout/header}}


<div class="container">
    <div class="row align-items-start">
        {{>layout/nav}}

        {{>layout/fileUpload}}
        <div style="padding-top:50px;"  class="col">
            <div class="container text-center">
                <h1 class="text-primary">{{#useremail}}{{useremail}}{{/useremail}}</h1>
                <h1 class="text-primary">{{#name}}{{name}}{{/name}}</h1>
                <h1><span class="alert alert-danger" />{{#name}}{{name}}{{/name}}여기는 MYPAGE 입니다</h1>
            </div>
        </div>
    </div>
</div>


{{>layout/footer}}